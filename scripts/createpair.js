import { ethers } from 'hardhat';

async function main() {
    // TODO: Create uniswap pair
    // Set the native pool within the treasury contract
}

main().catch((ex) => {
    console.error(ex);
    process.exitCode = 1;
});
